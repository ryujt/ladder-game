1. 참고 문서를 이용해서 프로젝트 생성 요청
2. 생성된 api 폴더를 참고해서 람다 코드 생성 요청
3. 지금까지 정보를 토대로 DynamoDB 생성 요청
4. 생성된 람다 정보를 토대로 api 폴더의 엔드포인트 수정 요청
5. 람다에서 헤더(CORS 포함) 관련 코드 모두 삭제 요청 (중복 처리됨, 현재 람다 설정에서 처리중)
6. 콘솔 로그를 추가해서 오류 추적이 쉽도록 처리해줘.